# Certificate Fixtures

These PEM files were generated with
`bt_ddos_shield_client.certificates.EDDSACertificateManager` and are committed so
tests use deterministic real keys.

- `validator_a.pem`
- `validator_b.pem`
- `validator_c.pem`
